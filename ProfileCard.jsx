import {useState} from "react";
export default function ProfileCard(){const [isEditing,setIsEditing]=useState(false);const [profile,setProfile]=useState({name:"Farrel Gaska",email:"farrel@example.com",phone:"081234567890"});const change=e=>setProfile({...profile,[e.target.name]:e.target.value});return <main className="center"><section className="card"><h1>Profile</h1>{isEditing?<form className="form" onSubmit={e=>{e.preventDefault();setIsEditing(false)}}><label>Name<input name="name" value={profile.name} onChange={change}/></label><label>Phone<input name="phone" value={profile.phone} onChange={change}/></label><button>Save</button></form>:<><p>Name: {profile.name}</p><p>Email: {profile.email}</p><p>Phone: {profile.phone}</p><button onClick={()=>setIsEditing(true)}>Edit</button></>}</section></main>}
