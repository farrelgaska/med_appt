import {useState} from "react";import {useNavigate} from "react-router-dom";
export default function SignUp(){const navigate=useNavigate();const [formData,setFormData]=useState({name:"",email:"",phone:"",password:"",role:"patient"});const [message,setMessage]=useState("");
 const change=e=>setFormData({...formData,[e.target.name]:e.target.value});
 const handleSubmit=async(e)=>{e.preventDefault();setMessage("");try{const response=await fetch("/api/auth/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(formData)});const data=await response.json();if(!response.ok)throw new Error(data.message||"Registration failed");localStorage.setItem("auth-token",data.authtoken);localStorage.setItem("auth-user",JSON.stringify(data.user));setMessage("Registration successful");setTimeout(()=>navigate("/login"),500);}catch(err){setMessage(err.message)}};
 return <main className="center"><form className="card form" onSubmit={handleSubmit}><h1>Create Account</h1><label>Name<input name="name" value={formData.name} onChange={change} required/></label><label>Email<input type="email" name="email" value={formData.email} onChange={change} required/></label><label>Phone Number<input type="tel" name="phone" value={formData.phone} onChange={change} required/></label><label>Password<input type="password" name="password" value={formData.password} onChange={change} minLength="6" required/></label><label>Role<select name="role" value={formData.role} onChange={change}><option value="patient">Patient</option><option value="doctor">Doctor</option></select></label>{message&&<p>{message}</p>}<button type="submit">Sign Up</button></form></main>}
