import {useState} from "react";import {BrowserRouter,Routes,Route,Navigate,Link} from "react-router-dom";import Navbar from "./components/Navbar";import Notification from "./components/Notification";import SignUp from "./components/Sign_Up";import Login from "./components/Login";import FindDoctorSearch from "./components/FindDoctorSearch";import GivesReviews from "./components/GivesReviews";import ProfileCard from "./components/ProfileCard";
function Home(){return <main className="hero"><div><span className="eyebrow">Healthcare for everyone</span><h1>Book trusted doctors from anywhere</h1><p>StayHealthy connects patients with doctors through a simple, accessible, and responsive experience.</p><Link className="cta" to="/appointments">Find a Doctor</Link></div></main>}
export default function App(){const [currentUser,setCurrentUser]=useState(()=>JSON.parse(localStorage.getItem("auth-user")||"null"));return <BrowserRouter><Navbar currentUser={currentUser} onLogout={()=>setCurrentUser(null)}/><Notification/><Routes><Route path="/" element={<Home/>}/><Route path="/signup" element={<SignUp/>}/><Route path="/login" element={<Login onLogin={setCurrentUser}/>}/><Route path="/appointments" element={<FindDoctorSearch/>}/><Route path="/reviews" element={<GivesReviews/>}/><Route path="/profile" element={<ProfileCard/>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></BrowserRouter>}
