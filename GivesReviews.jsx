import {useState} from "react";
export default function GivesReviews(){const [review,setReview]=useState("");const [rating,setRating]=useState("5");const [isSubmitted,setIsSubmitted]=useState(false);const submit=e=>{e.preventDefault();if(!review.trim()||isSubmitted)return;setIsSubmitted(true)};return <main className="center"><form className="card form" onSubmit={submit}><h1>Give Review</h1><label>Rating<select value={rating} onChange={e=>setRating(e.target.value)} disabled={isSubmitted}>{[5,4,3,2,1].map(n=><option key={n}>{n}</option>)}</select></label><label>Review<textarea value={review} onChange={e=>setReview(e.target.value)} disabled={isSubmitted} required/></label><button disabled={isSubmitted}>{isSubmitted?"Review Submitted":"Submit Review"}</button></form></main>}
